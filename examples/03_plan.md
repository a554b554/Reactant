# Plan Skill Test

## Test 1: Basic plan — ask for improvement suggestions

Our system allows users to write annotations inline. These annotations are then processed by an agent. The agent applies changes to the document.
<@plan: how to make this paragraph more engaging and specific>

## Test 2: Plan with structural request

In this paper, we present EAML. EAML is a language. It supports inline annotations. Users can define skills. Skills are executed by agents. The results appear in the document.
<@plan: restructure into two coherent paragraphs with better flow>
